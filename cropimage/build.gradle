apply plugin: 'android-library'

android {
    compileSdkVersion 17
    buildToolsVersion "19.0.1"

    defaultConfig {
        versionCode 1
        versionName "1.0"

        minSdkVersion 8
        targetSdkVersion 17
    }
}
